# Todo

- [ ] Rename `DiffThis.AI.OpenAI` project to `DiffThis.AI.Providers` — update folder, `.csproj`, assembly/root namespace, all `ProjectReference` entries, and `namespace` declarations across all files in the project.
